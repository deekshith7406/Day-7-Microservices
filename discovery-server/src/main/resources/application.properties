spring.application.name=DISCOVERY-SERVER
server.port=8761

# This server IS the registry, so it doesn't need to register itself with
# another registry or pull a copy of someone else's registry.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
